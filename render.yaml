services:
  - type: web
    name: data-advisor-public
    runtime: python3.12  # Forceer Python 3.12
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT
